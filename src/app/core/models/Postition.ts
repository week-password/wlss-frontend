export enum EPosition {
  bottom = 'bottom',
  left = 'left',
  right = 'right',
  top = 'top',
}

export enum ETextPosition {
  center = 'center',
  end = 'end',
  justify = 'justify',
  left = 'left',
  right = 'right',
  start = 'start',
}
